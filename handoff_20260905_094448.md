# Handoff — ShiftEase session 2026-09-05 (09:44)

Tài liệu chuyển giao cho phiên sau. **Không duplicate** nội dung đã có trong
plan/result — tham chiếu path. Bối cảnh tối thiểu + trạng thái thật + việc
đang treo + các bẫy đã học.

---

## 1. Dự án & môi trường

| Mục | Giá trị |
|---|---|
| Repo (thật, theo user) | `/Users/hoang/htdocs_apps/ShiftEase` |
| Repo (sandbox — nơi agent làm việc) | `/home/dinhxhoang08/htdocs_apps/ShiftEase` |
| **Không phải git repo** (sandbox không có `.git`) | không commit/push được ở đây |
| Stack | Pure **Dart** package (KHÔNG Flutter SDK dependency trong pubspec) — chỉ `sqlite3` + `timezone`; **chưa có UI code** (`lib/` chỉ có `core/`) |
| Verification | `dart analyze` · `dart test test/core/` (**155/155 pass**) · 3 verify scripts Node |
| Ngôn ngữ hồ sơ | Tiếng Việt (plan/result/features.md) — code/comments tiếng Anh |

## 2. Kiến trúc & spec (đã chốt — tham chiếu, không viết lại)

- `.plan/plan1_final_v2.md` — spec gốc (mục 10 = Architectural Invariants + NEVER list, **không được sửa/nới lỏng**)
- `.plan/plan2.md` — kỹ thuật chi tiết (gồm §5.7 Weekly Overtime Allocation LIFO)
- `.plan/plan3_final.md`, `.plan/plan3_final_v2.md` — Gate 0 pattern/time
- `.plan/plan4_gate3_persistence.md` — spec Gate 3 (đã duyệt, đã implement)
- `.plan/features.md` — **danh mục tính năng App+UI** (đã cập nhật cuối session: 155/155, 6 CI jobs, row Persistence)
- `prompt*.md` (root) — các prompt review vòng theo từng gate (user gửi để agent làm theo)

Invariants: INVARIANT-001..008 — bảng đầy đủ ở `.plan/features.md` §6.

## 3. Tiến độ Gates (tất cả code core, KHÔNG UI)

| Gate | Module | Trạng thái | Báo cáo |
|---|---|---|---|
| 0 | `core/time`, `core/pattern` | ✅ | `result5_gate0.txt` |
| 1 | `core/money` (+ integration pattern→money) | ✅ | `result6_gate1.txt` |
| 2 | `core/import` (+ integration import→money) | ✅ | `result7_gate2.txt`, `result8_gate2_integration.txt` |
| 3 | `core/db` SQLite (v2 schema) | ✅ | `result9_gate3.txt` (§7a P1 chain fix, §7b db→money, §7c db→pattern, §7d CREATE convention) |
| Review round 2 | toàn integration suite | ✅ | `result10_review_round2.txt` |

**155 tests =** time 32 · pattern 46 · property 7 · money 24 · import 15 · db 12 · integration 19.
Golden JSON: `test/golden/{time_engine,money_engine,import_pipeline}_cases.json` + `scripts/verify_*.mjs`.

### Chi tiết Gate 3 (quan trọng cho phiên sau)
- `lib/core/db/db.dart` — `sqlite3` FFI, `PRAGMA user_version`, **schemaVersion = 2**. v2 = `ALTER TABLE overrides ADD COLUMN patternId TEXT` (CREATE convention, v1→v2 upgrade path tested).
- Repos: `pattern_repository.dart` (jobs/templates/patterns+sequence), `schedule_repository.dart` (occurrences, override log **append-only**, snapshot INVARIANT-006, `renderJobSchedule`), `business_repository.dart` (PayRule versioned + ImportSession), `override_codec.dart` (6 payload types <-> JSON).
- `renderJobSchedule` admission pass (chronological): targeted ops theo baseline/chain root; **CREATE admit khi patternId được render + payload date trong range**; id CREATE gia nhập allowed-root để chain; orphan surface issue — không silent.

## 4. Việc ĐANG TREO (chờ user, theo thứ tự)

1. **Duyệt D-1/D-2** (schema v2 + CREATE convention official) — đã trình trong `result9_gate3.txt` §7d + `result10_review_round2.txt` §6. User chưa phản hồi duyệt.
2. **Mở Gate UI layer (Flutter) M1 Basic Calendar** — user đã yêu cầu ("Mở gate UI layer (Flutter) theo roadmap M1"). Survey mới bắt đầu:
   - pubspec KHÔNG có Flutter SDK; `lib/` chỉ `core/`. **Cần probe: flutter SDK có trong sandbox không** (lệnh `which flutter` trước đó timeout).
   - Roadmap: M0 (engines ✅) → M1 Basic Calendar (UI + persistence) → M2 Import UI → M3 Multi-Job+Pay UI → M4 Export/Share → M4.5 OCR spike gate → M5 OCR → M6 Cloud.
   - `.plan/features.md` §4 = danh mục màn hình UI (P0: Today, Calendar Week/Month, Quick Add/Add Shift, Pattern Builder, Occurrence Edit, Jobs, Pay Rules, Settings, DST Dialog, Unable-to-calculate).
3. Integration db→pattern / db→money đã có; **chưa có** domain service layer giữa repo & UI (helpers trong test đang "chốt contract" cho calendar layer tương lai).

## 5. Bẫy & bài học (đọc kỹ — user rất khắt khe)

- **User KHÔNG tin lời báo cáo**: mỗi "done" phải kèm bằng chứng — dòng code + output test + `ls -la` file trên đĩa. Lịch sử có 4+ vòng user phát hiện file CI "báo đã tạo" nhưng không tồn tại trên đĩa. **Luôn chạy `ls`/`find` xác nhận trước khi báo cáo.**
- Không tự ý bỏ qua/làm nhẹ lỗi; surface deviation kèm lựa chọn + lý do, chờ duyệt.
- Sửa file thật ở **sandbox path** đúng nơi đang làm việc.
- User review theo từng vòng bằng file `prompt_*.md` mới; trả lời đúng trọng tâm từng việc, dán nguyên văn đoạn sửa để đối chiếu.
- Tiếng Việt: **cẩn thận encoding** — từng có sự cố mất dấu/đổi nghĩa khi dùng script tự động xử lý text hàng loạt. Sửa thủ công từng chỗ.
- Golden test là nguồn sự thật; khi phát hiện expected sai → sửa kèm ghi chú + chờ duyệt (tiền lệ PAY-013, SH-002, IMPORT amendments).

## 6. Trạng thái verification cuối session

```bash
dart analyze                        → No issues found!
dart test test/core/                → 00:05 +155: All tests passed!
node scripts/verify_{all,money,import}_cases.mjs → 0 errors
```

## 7. Suggested skills (agent phiên sau nên invoke)

- `verification-before-completion` — bắt buộc trước mọi claim "xong/pass"
- `code-review` — trước khi mở gate mới / sau mỗi gate (pattern user đang dùng)
- `systematic-debugging` — khi user gửi prompt phát hiện bug
- `database` — khi chạm schema/migration/repo SQLite (Gate 3 trở đi)
- `writing-plans` / `executing-plans` — khi mở Gate UI (M1) hoặc viết spec gate
- Nếu mở UI Flutter: `flutter-coding`, `architecture`, `design-system`, `project-orchestrator` (nhóm skill Flutter) — sau khi xác nhận Flutter SDK khả thi trong sandbox

## 8. Files tham chiếu chính (không duplicate ở đây)

- Báo cáo gate: `result5..result10` (root)
- Spec: `.plan/plan1_final_v2.md`, `plan2.md`, `plan3_final_v2.md`, `plan4_gate3_persistence.md`, `features.md`
- Code: `lib/core/{time,pattern,money,import,db}/`
- Tests: `test/core/{time,pattern,money,import,db,property,integration}/`, `test/golden/*.json`
- CI: `.github/workflows/test.yml` (6 jobs — file THẬT, đã ls xác nhận)
