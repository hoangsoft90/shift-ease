# Handoff — ShiftEase — 2026-09-07 (post Gate C + code review)

> Đọc file này trước tiên. Trạng thái: **Gate C ĐÃ ĐÓNG sau code review**.
> Bằng chứng: `result15_gate_c.txt` (trên đĩa, `ls` xác nhận — 11349 bytes).
> Phiên trước: `handoff_20260906_144500.md` (Gate A/B).

## 1. Trạng thái cuối (evidence — không tin lời, tin file)

- `flutter analyze --no-pub` → **No issues found**
- `flutter test test/` → **207/207 pass, exit=0**
  - core 176 (time 32 · pattern 48 · property 7 · money 24 · import 18 · db 24 · integration 19 · adversarial 4)
  - domain 9 (M1 3 + AC-1 1 + changeRosterFrom 2 + income estimate 3)
  - UI 16 (M1 2 · M1b 4 · M2 5 · DST 3 · Pay/Income 2)
  - features 6 (ICS 2 · reminder 4)
- `result15_gate_c.txt` trên đĩa (`ls -la` xác nhận) — gồm cả **Addendum post-review**.

## 2. Gate C làm gì (prompt_gate_c.md — từng task theo thứ tự §6)

**Phần A — Integrity** (mỗi task = code + test riêng):
- **A1** `commitImport` chỉ REVIEWING→COMMIT; EXTRACTED reject; zero-approved reject — `lib/core/import/import_engine.dart` ~L403–460; test `import_engine_test.dart` L586/L629 (import 18).
- **A2** `changeRosterFrom` 1 transaction (close-old + save-new; fail → rollback, không roster mồ côi) — `schedule_service.dart` L396–420 BEGIN/COMMIT/ROLLBACK + `pattern_repository.dart` L151 `savePatternInTransaction` (caller owns txn); test `schedule_service_test.dart` L235/L284 + ADV-2.
- **A3** `saveOverride` immutability: same id + payload giống = idempotent; khác → `ImmutableHistoryError` — `schedule_repository.dart` L250/255 + `_sameOverrideContract`; test db_test L436/L464 + CREATE patternId-refuse.
- **A4** ImportSession COMMITTED immutable — `business_repository.dart` L269–283 + `_sameCommittedSessionPayload` (so JSON từng field + candidates multiset); test db_test L671/L700 (ADV-5, ADV-5b).
- **A5** `test/core/adversarial/gate_c_adversarial_test.dart` (ADV-1..5, 4 tests) + full regression 207/207.

**Phần B — Product:**
- **B1** DST dialog (D-C5): `lib/features/common/dst_resolution_dialog.dart` + gate trong `occurrence_sheet.dart` `_dstGateError` (CREATE + UPDATE). AMBIGUOUS hiện 2 candidate (offset/abbr), **không auto-pick, acknowledging vẫn không ghi**; NONEXISTENT chặn + giải thích. Test `test/ui/dst_resolution_flow_test.dart` (3).
- **B2** Pay/Income tối thiểu (D-C6): `lib/domain/income_estimate.dart` (UNAVAILABLE kèm lý do khi thiếu rule / đa version / multi-WEEK-scheme — không đoán) + `estimateIncome` trên service + `lib/features/pay/pay_rule_edit_dialog.dart` + income card Today nhãn **"Ước tính — không phải bảng lương chính thức"** + Pay rule card JobDetail. Test `income_estimate_test.dart` (3: $1505 golden, compose, UNAVAILABLE) + `pay_income_flow_test.dart` (2).
- **B3** ICS offline (D-C7): `lib/features/export/ics_export.dart` (RFC 5545, UTC VEVENT, escape, CRLF) + `ics_saver.dart` (path_provider, fallback temp) + action "Export week (.ics)" JobDetail. Test `ics_export_test.dart` (2).
- **B4** Reminder baseline: `lib/features/notifications/shift_reminder.dart` — pure `nextReminderSpec` (60′ lead, next shift, skip-past, sort) + guarded wrapper; wire TodayScreen initState. Test `shift_reminder_test.dart` (4).
- **B5** Platform: pubspec `path_provider ^2.1.4` + `flutter_local_notifications ^17.2.4`; tạo `android/` + `ios/`. **KHÔNG build apk local** (chỉ đạo user — tuyệt đối giữ nguyên trong sandbox).
- **B6** result15 trên đĩa · **B7** docs đồng bộ (checklist.md / .plan/features.md / next.md — đều 207).

## 3. Code review post-Gate C (4 finding — ĐÃ SỬA + re-verify 207/207)

| # | Mức | Finding → Sửa |
|---|---|---|
| 1 | Medium | Pay dialog: same-id khác-payload → StateError unhandled, không feedback + multiplier<1 bị silently đổi 1.5 → **try/catch hiện lỗi trên form; từ chối multiplier<1 rõ ràng** |
| 2 | Low-Med | Reminder dùng id mới mỗi sync → nhân bản → **fixed id 1001, re-schedule = replace** |
| 3 | Low | TodayScreen comment sai về thời điểm resync → **sửa comment + ghi known limitation** |
| 4 | Cosmetic | `ensureJob` 2 câu dính 1 dòng → format lại |

Không-chặn (cho M3): night/weekend parse-garbage→0 âm thầm bỏ differential (editor tối thiểu); ICS không fold dòng >75 octet; `_incomeCard` tính đồng bộ trong build; A1 nhánh unresolved không append history item (vẫn set error state).

## 4. Việc tiếp theo (theo `next.md` — đã cập nhật)

1. **Human review Gate C** — duyệt `result15_gate_c.txt` + diff từng file + log test.
2. Sau review: **M3 mở rộng** (plan nháp: `.plan/plan8_m3_income.md` — income dashboard đầy đủ + template library picker) và/hoặc **M2b** (CSV mapping UI + Re-import diff UI).
3. Device/CI (không làm trong sandbox): build apk thật, notification permission flow, ICS share sheet.

## 5. Kỷ luật cho phiên sau (bám kế hoạch — các phiên trước đã vi phạm)

1. **Không báo "xong" bằng lời** — mỗi task phải có: dòng code (file + vùng) + test pass (số cụ thể) + `ls` xác nhận file report trên đĩa.
2. **Viết result*.txt TRƯỚC khi sửa docs** — checklist/features không được khẳng định file chưa tồn tại (lần này checklist ghi "ls xác nhận" trước khi file có thật).
3. **Tuyệt đối không `flutter build apk` local** — sandbox không có toolchain; build chỉ ở CI/máy user.
4. Không BEGIN lồng nhau trong SQLite — variant `*InTransaction` (caller owns txn) khi đã ở trong giao dịch.
5. Đồng bộ 3 file docs ngay khi số test đổi; số lịch sử giữ kèm "tại thời điểm …".
6. Mỗi lần sửa code: chạy analyze + nhóm test liên quan NGAY, full suite trước khi chốt.

## 6. Kỹ thuật không chặn / ghi chú

- `committedOffDatesInRange` O(days × sessions) — ổn với roster vài tháng.
- Session committed từ data dev cũ (pre-window cột NULL) không được consult cho suppression — không có production data.
- DST dialog `selectedIndex` luôn null (informational theo D-C5 — payload không lưu "pass nào").
- Sandbox không có git repo — review trực tiếp trên đĩa; khi về repo thật hãy `git diff` Gate C theo file list ở §2.
