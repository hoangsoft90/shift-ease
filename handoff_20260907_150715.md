# Handoff — ShiftEase — 2026-09-07 (plan8 RC batch — đang giữa chừng)

> Đọc file này trước tiên. Trạng thái: **plan8 Release Candidate đang chạy — Phần A+B xong, C1/C2 pure xong (UI dở), D–J chưa làm**.
> Bằng chứng: `result16_rc_progress.txt` (trên đĩa — `ls` xác nhận) + `result15_gate_c.txt` (Gate C đã đóng).
> Phiên trước: `handoff_20260907_093210.md` (Gate C + code review). Plan: `.plan/plan8_m3_income.md` (§15 = thứ tự thực thi).

## 1. Trạng thái cuối (evidence — không tin lời, tin file)

- `flutter analyze --no-pub` → **No issues found** (chạy 15:0x, sau mọi thay đổi code)
- `flutter test test/` → **249/249 pass, exit=0**
  - core **199** (time 32 · pattern_engine 48 · property 7 · money 24 · db 28 · import 30 · integration 26 · adversarial 4)
  - domain **18** (schedule_service 7 · income_estimate 10 · import_composition 1)
  - UI **26** (M1 2 · M1b 4 · M2 5 · DST 3 · Pay/Income 2 · write_error 4 · breakdown 3 · impact 2 · preset 1)
  - features **6** (ICS 2 · reminder 4)
- Gate C = 207 → +42 test. Regression 0 giữa các phase (A xong 229/229 → B+C pure 249/249).

## 2. Phiên này làm gì (plan8 §15 — A → B → C pure)

**Phần A — Integrity (A1–A8 ✅, mỗi task = code + test):**
- **A1** `lib/features/common/write_guard.dart` (runWrite/runWriteAsync/writeErrorMessage) — wrap mọi write-path: occurrence_sheet, quick_add_sheet, import_screen, template_edit_dialog, pattern_builder_screen, job_detail_screen. Test `write_error_boundary_test.dart` (4: unit 2 + widget 2).
- **A2** imported-only CREATE: `pattern_types.dart` `importedSchedulePatternId(jobId)` sentinel `__imported__:<jobId>` + admission trong render + `override_actions.dart`. Test `rc_imported_create_test.dart` (3).
- **A3** `schedule_repository.dart` `saveOccurrences` → scoped REPLACE (xóa stale trong scope, giữ snapshot khác job). Test `rc_materialized_occurrences_test.dart` (4).
- **A4** verify import state machine: thêm ERROR→COMMIT + COMMITTED→COMMIT reject (import_engine_test +2).
- **A5** conflict validation: `import_types.dart` `ErrorCodes.conflict='CONFLICT'` + engine từ chối OFF+SHIFT cùng ngày / duplicate identity, zero rows (import_engine_test +3).
- **A6** override canonical compare reordered-payload = idempotent (db_test +1).
- **A7** atomic re-version: fail-before (close fail → 0 write) + survives restart (schedule_service_test +2).
- **A8** PayRule integrity: `business_repository.dart` `savePayRule` — unknown jobId **throw (bỏ auto-create job ảo)**, negative/malformed reject, children compare order-independent (db_test +3).

**Phần B — Income (B1–B7 ✅):**
- **B1/B2** `income_estimate.dart`: per-occurrence rule resolve; HOURS_IN_WINDOW parity engine (PAY-013 5h / overnight 8h / DST 7h goldens); WEEK OT vắt 2 version → UNAVAILABLE + reason (income_estimate_test +5).
- **B3/B4** `lib/features/income/income_breakdown_screen.dart` (Date/Range · Hours · Regular/Differentials/OT/Total · disclaimer). Test `income_breakdown_flow_test.dart` (2).
- **B5** `lib/features/pay/pay_presets.dart` (US-CA/US-NY/US-TX/UK-NHS/DE) + wire PayRuleEditDialog (prefill; Save mới persist). Test `pay_preset_flow_test.dart` (1).
- **B6** Income Impact persist:false — `IncomeImpact` + service `currentWeekShifts`/`estimateIncomeImpact`/`estimateReversionImpact` + impact line OccurrenceSheet (CREATE/EDIT/DELETE dialog) + re-version preview pattern_builder. **Đã sửa thiếu sót thật: CREATE form chưa wire impact line** (test phát hiện). Test income_estimate_test (+2) + `income_impact_flow_test.dart` (2: $1680.00 → $1920.00 +$240.00; no-rule → reason).
- **B7** Today income card multi-job total — chỉ cộng AVAILABLE, exclude nêu tên (breakdown_flow +1).

**Phần C — C1/C2 (🔧 pure xong, UI CHƯA):**
- **C1** `lib/core/import/import_validation.dart` `validateCsvMapping` (unmapped required / duplicate / missing times / DST gap) — 4 test.
- **C2** `lib/core/import/import_diff.dart` `computeRosterDiff` (added/removed/modified/unchanged + old→new + hoursDelta) — 3 test.
- Test: `test/core/import/rc_csv_diff_validation_test.dart` (7).

## 3. Việc tiếp theo (theo §15 — làm đúng thứ tự)

1. **C1 UI**: service `parseCsv` (ScheduleService, giống parsePaste nhưng sourceType csv + columnMapping) → ImportScreen thêm mode CSV: paste CSV → Preview rows (header + dòng đầu) → map columns (date/start/end/shiftType dropdown, auto-detect khi null) → validate card (`validateCsvMapping`) → tạo session → review (UI có sẵn). Commit vẫn qua Review.
2. **C2 UI**: committed-roster rows (service helper qua `schedule_repository.importedOccurrencesInRange` + localWallTime) → diff card trước Review: +/−/~/= theo ngày, modified old→new, `Hours: +Xh`, income impact (khi payEnabled; UNAVAILABLE → reason). 
3. **D** DST UX wording "Choose another time" + test start/end.
4. **E1/E2** stale reminder cancellation + lifecycle resync · **E3** permission (device).
5. **F** Backup/Restore · **G** SQLCipher · **H** Settings · **I** mobile readiness · **J** adversarial.
6. **Evidence** `result16_rc.txt` (VIẾT TRƯỚC khi sửa docs) → đồng bộ checklist.md/.plan/features.md/next.md → handoff mới.
7. Sau batch: human review RC → mở deferred §14 hoặc closed testing.

## 4. Cần hỏi user (đã ghi trong checklist.md / .project/state.md)

1. Tiếp tục RC §15 hay checkpoint review ngang sau mỗi phase?
2. E3/G/I (device/CI — permission flow, SQLCipher native, build thiết bị) giữ trong RC dạng structure + note BLOCKED hay tách khỏi RC?
3. Sau RC: mở deferred (OCR/M4.5 spike — cần 20–30 roster thật · Cloud · Sharing) hay closed testing trước?

## 5. Kỷ luật (bám — các phiên trước đã vi phạm)

1. Không báo "xong" bằng lời — mỗi task: dòng code (file + vùng) + test pass (số cụ thể) + `ls` xác nhận file trên đĩa.
2. **Viết result*.txt TRƯỚC khi sửa docs** — `result16_rc.txt` CHƯA tồn tại; đừng khẳng định trước khi `ls`.
3. Tuyệt đối không `flutter build apk` local — build chỉ ở CI/máy user.
4. Không BEGIN lồng nhau trong SQLite — variant `*InTransaction` khi đã trong giao dịch.
5. Đồng bộ 3 file docs khi số test đổi; số lịch sử giữ kèm "tại thời điểm …".
6. Khi widget test phát hiện code thiếu → sửa code thật, không sửa test cho qua (tiền lệ B6 CREATE impact line phiên này).
7. Chạy analyze + nhóm test liên quan NGAY sau mỗi sửa; full suite trước khi chốt phase.

## 6. Ghi chú kỹ thuật / không chặn (đã biết)

- Service CHƯA có `parseCsv`/committed-roster diff helper (C1/C2 UI cần thêm). `importedOccurrencesInRange(jobId, from, to)` + `localWallTime(o)` + `committedOffDatesInRange` có sẵn trong repo/service.
- Range diff C2 nên lấy theo new-window (min/max date candidate); tránh gọi `committedOffDatesInRange` với range rộng 8 triệu ngày (nó duyệt từng ngày).
- Android/ios folders + path_provider + flutter_local_notifications đã có (Gate C B5); không build local.
- Docs ngoài đã đồng bộ trạng thái RC mid-batch: `checklist.md`, `.plan/features.md`, `next.md`, `result16_rc_progress.txt`, `.project/{state,working,openspec_entry,ai-rules}.md`.
